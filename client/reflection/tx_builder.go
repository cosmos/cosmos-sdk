package reflection

// TxBuilder implements a raw transaction builder
// it of course cannot offer the full set of functionalities
// since it cannot deduct the interface execution
type TxBuilder struct {
}
