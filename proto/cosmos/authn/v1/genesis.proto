syntax = "proto3";
package cosmos.authn.v1;

import "cosmos/authn/v1/authn.proto";

message GenesisState {
    // params defines all the parameters of the module.
    Params params = 1;

    // accounts are the accounts present at genesis.
    repeated Account accounts = 2;
}
