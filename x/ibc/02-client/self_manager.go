package client

/*
import (
	"github.com/cosmos/cosmos-sdk/store/mapping"
)

type SelfManager struct {
	Manager
}

func NewSelfManager(base mapping.Base) SelfManager {
	return SelfManager {
		Manager : NewManager(base.Prefix([]byte("/protocol")), base.Prefix([]byte("/free")))
		Manager {
		protocol: mapping.NewMapping(base, []byte("/protocol")),
		idval: mapping.NewValue(base, []byte("/id")),
		idgen: IntegerIDGenerator
	}
}
}
*/
